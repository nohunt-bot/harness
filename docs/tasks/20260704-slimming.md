# Task: Slimming round — zero behavior change, zero info loss (archive, verify, anti-bloat)

Status: active (2026-07-04)
Branch: slim/20260704 — DO NOT merge to main without user confirmation
(user instruction; also stop before any "restart"-equivalent).

## Acceptance criteria
1. Always-loaded set (CLAUDE.md, MEMORY.md index, hook output, command
   descriptions) verified current; stale data fixed with evidence; bytes not
   larger than baseline.
2. Archive-only: no file deleted; historical narrative moved to `archive/`
   (repo) or `memory/archive/` (memory dir); every mover leaves resolvable
   references (updated links or conclusion stub); LESSONS.md fully preserved.
3. Reconciliation done BEFORE any code-adjacent change: repo ↔ ~/.claude disk
   ↔ runtime (symlinks/install --check/registered hooks); drift committed
   first. Result: ZERO drift — CLAUDE.md/settings/harness are symlinks;
   agents/commands/scripts are per-file symlinks inside real dirs (by
   design, install.sh expected_links); --check OK incl. MCP registration;
   no tracked junk (.gitignore already covers .DS_Store/.idea/*.bak/backups).
4. Skills needed by this project live in the repo (skills/ or commands/) —
   verified; none exist only in ~/.claude.
5. No large-file split needed (largest script 6.3 KB) — evidenced by the
   interface baseline; script interfaces byte-identical at the end.
6. Anti-bloat mechanisms added so long-term use doesn't inflate always-loaded
   tokens (growth-vector checks in harness-health; proposals lifecycle).
7. One commit per step, each with a rollback command; final report: before/
   after table + full health check; STOP before merging.

## Plan
- [x] S0: branch + baselines (footprint, script interfaces) + task file → receipts
- [x] S1: reconciliation report (repo↔disk↔runtime) + install.sh copy-drift check read → findings in this file
- [x] S2: always-loaded verify+slim: memory files verified (stale → memory/archive/ + index update), CLAUDE.md verified → byte counts
- [x] S3: repo archive moves: LETTER.md, CLAUDE.md.bak, resolved proposals → archive/; refs updated; .DS_Store/.idea hygiene → grep zero dead refs
- [x] S4: stale-data fixes in routed/maintenance docs (README numbers etc.) → each fix cites evidence
- [ ] S5: anti-bloat mechanisms (harness-health growth checks; proposals lifecycle note) → wired + referenced paths exist
- [ ] S6: closeout: script-interface diff empty, footprint after-run, before/after table, install --check + health sweep, STOP for user merge confirmation

## Progress log
- 2026-07-04 | S0 | branch slim/20260704; baselines: slim-before.txt + script-interfaces.txt captured pre-change
- 2026-07-04 | S1 | reconciliation: zero drift (per-file symlinks verified by ls -la; install --check OK; git ls-files: no tracked junk). Earlier "copies" reading was a directory-level misread — corrected. install.sh symlink open question: MOOT (already symlinks)
- 2026-07-04 | S2 | always-loaded verified CURRENT, zero stale bytes: CLAUDE.md 3,046 B (E26/E27 evidence); 3 memory files checked against reality — presentation HTML exists (38,805 B), /Users/ch/md-editor + task file exist, ~/.codex absent (matches memory claim). Nothing archived: all three are live context; index already 569 B. Memory layer edits: none needed
- 2026-07-04 | S3 | archive/proposals/ created; 2 resolved proposals git-mv'd (shas preserved: 29fc1760439e, ba4bb9947170); 5 referencing files updated (dispatch §6, LESSONS, hypothesis log, onprem task, portability decision); CLAUDE.md.bak → backups/ (sha 0487628c88d9 matches 20260703 record); archive/README.md sha=b05ce22ccbc7. LETTER.md + DIAGNOSIS.md stay (decision: 8 live refs incl. gated files, zero load cost). Dead-ref grep: clean
- 2026-07-04 | S4 | stale-data audit: README fully cross-checked — plugins claim = settings.json exactly (3 plugins, jq read-back), hooks rows = PreToolUse+SessionStart, templates/README = 5+1 current files, cross-agent table matches disk (~/.codex absent, hermes unwired), no stale numbers. ONE gap: Layout table missing archive/ + docs/experiments/ rows → added. MAINTENANCE/pack refs already verified by E27 rerun

## Decisions
- docs/tasks/*.md stay in place even though historical: pack05 §4 says task
  files are never compacted/moved (they are already never-loaded history);
  moving them saves 0 tokens and breaks the rule. Same for docs/DIAGNOSIS.md:
  actively referenced as rationale by live rules, never auto-loaded — stays.
- Whole-file archive moves update referencing links instead of leaving stubs
  (zero-loss, no stub clutter); content extracted FROM living docs leaves
  conclusion + link in place.

## Open questions (batched for merge checkpoint)
- (S1 resolved the install.sh question: already per-file symlinks.)
